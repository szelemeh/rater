import { RateSearchPipe } from './rate-search.pipe';

describe('RateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
