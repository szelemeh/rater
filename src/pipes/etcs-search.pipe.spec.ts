import { EtcsSearchPipe } from './etcs-search.pipe';

describe('EtcsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new EtcsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
