import { SemesterSearchPipe } from './semester-search.pipe';

describe('SemesterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SemesterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
